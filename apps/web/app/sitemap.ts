import type { MetadataRoute } from 'next';
const paths = ['/', '/generator', '/scanner', '/projects', '/privacy', '/about', '/qr-code-generator', '/url-qr-code', '/wifi-qr-code', '/vcard-qr-code', '/whatsapp-qr-code', '/email-qr-code', '/text-qr-code', '/image-qr-code', '/pdf-qr-code', '/social-media-qr-code', '/qr-code-with-logo', '/qr-code-designer', '/qr-code-scanner', '/batch-qr-code-generator', '/dynamic-qr-code'];
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'; return paths.map((path) => ({ url: `${base}${path}`, changeFrequency: path === '/' || path === '/generator' ? 'weekly' : 'monthly', priority: path === '/' ? 1 : path === '/generator' ? 0.95 : 0.7 })); }
