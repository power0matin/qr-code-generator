import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Privacy', description: 'How ModuQR keeps static QR generation and local projects private.', alternates: { canonical: '/privacy' } };
export default function PrivacyPage() { return <article className="page page-narrow"><span className="eyebrow">Privacy first</span><h1>Your static QR data belongs in your browser.</h1><p>ModuQR&apos;s static studio does not require an account. QR payloads, WiFi passwords, uploaded logos, scanned images, and saved projects are processed locally by the browser. The static studio does not need those values on a server to generate a QR code.</p><h2>Local projects</h2><p>Projects are stored in IndexedDB on your device. Clearing site data can remove them, so export the design JSON when you need a portable backup.</p><h2>Uploads</h2><p>Logo and scanner inputs are read by browser APIs. SVG logos are sanitized before being embedded. Scanner images are decoded locally and are not automatically opened as URLs.</p><h2>Cookies and tracking</h2><p>The current static application has no required analytics or advertising cookies. Theme preference is kept in local storage.</p><h2>Sharing</h2><p>The current static application does not place project payloads into shareable links. This prevents WiFi passwords and other sensitive QR content from accidentally entering browser history through a generated share URL.</p></article>; }
